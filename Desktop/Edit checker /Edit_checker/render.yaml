services:
  - type: worker
    name: telegram-edit-checker-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false